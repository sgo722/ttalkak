consumer:
  topics:
    create-compute:
      name: create-compute

    create-user:
      name: create-user

    command-deployment-status:
      name : command-deployment-status

    rebuild-compute:
      name : rebuild-compute
    
    create-database:
      name: create-database